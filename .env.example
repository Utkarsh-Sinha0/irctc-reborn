# Session secret for signing booking/session cookies (HMAC).
# Provide a long random string in production deployments.
# Dev fallback exists but is documented on /how-it-works as mock-only.
SESSION_SECRET=
